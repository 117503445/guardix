version: '3'

tasks:
  default:
    desc: "The default task" 
    # deps: [deploy]
    cmds:
      - clear
      - task: run
      
  run:
    deps:
      - build
    cmds:
      - ./scripts/run.sh

  build:
    cmds:
      - ./scripts/build.sh